---
id: GR-NNN
title: <short imperative — "Ship Show-Diagnostic command", not "showdiag work">
status: OPEN
phase: <project phase number or "ongoing">
opened: YYYY-MM-DD
closed:
linked-drs: []
linked-plan: n/a
---

# GR-NNN — <Title>

> This is a TEMPLATE. Copy to `GR-NNN-slug.md` and fill in. Do not edit this file in place.

## Why

<one or two paragraphs: what motivates this goal — the user pain, the strategic reason, the constraint it satisfies. Plain English. No jargon. This is the part future-Keith will read first.>

## Acceptance criteria — autonomous (`/goal` drives these)

<Claude-verifiable items only. Each must be checkable from a tool call Claude can run unaided: file content, exit code, test count, frontmatter value, git state. These get rendered into the `/goal` condition string.>

- [ ] <criterion 1>
- [ ] <criterion 2>
- [ ] <criterion 3>

## Acceptance criteria — human-gated (STOP loop, hand off to Keith)

<Items requiring Keith to click, paste, log in, generate a token, check a web UI, or run an interactive flow. NEVER rendered into the `/goal` condition string. On reaching one of these mid-loop, `/goal` prints instructions, logs the handoff, and exits — it does NOT poll. Omit this section if all criteria are autonomous.>

- [ ] <human-gated item 1 — or remove this section entirely if none apply>

## Non-goals

<what this goal explicitly does NOT include — protects against scope creep.>

## Linked work

- Plans: <if any plan files exist, point at them>
- Decisions: <DRs this goal depends on or motivates>
- Phase: <if part of a larger phase plan>

## Status log

<append-only. One line per status change with date and evidence.>

- YYYY-MM-DD opened OPEN
